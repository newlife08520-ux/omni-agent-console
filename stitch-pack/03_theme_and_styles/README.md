# Theme & Styles

- `index.css` — Tailwind layers、**light theme** CSS variables（`--primary`、`--radius`、chart 色等）。
- `tailwind.config.ts` — 內容路徑含 `client/src`。
- `components.json` — shadcn 設定（aliases、CSS 路徑）。

主畫面另見 `App.tsx` 內聯語意色：側欄 `stone-800`、主內容 `bg-[#faf9f5]`。
