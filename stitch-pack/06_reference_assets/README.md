# Reference Assets

本專案 **logo** 多由後台設定 `logo_url` 動態載入（見 `App.tsx`／`app-sidebar.tsx`），`client/public` 可能為空。

請在執行 `npm run dev` 後：

1. 將實際顯示之 logo、關鍵插圖（若有）匯出為 PNG／SVG；
2. 放入本資料夾；
3. 於 `PACKING-SUMMARY.md` 更新清單。

**勿**將含商業秘密、客戶商標之未授權素材提交至外部工具。
