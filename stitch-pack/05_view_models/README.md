# View models（給 Stitch 理解資料形狀）

## 來源說明

- **完整型別**在 repo 的 `shared/schema.ts`（**未**複製進本包，避免與後端型別庫重複且過大）。
- 本目錄僅含 **前端已使用之常數映射** 與 **去敏感 JSON 示例**。

## 檔案

| 檔案 | 用途 |
|------|------|
| `contact-schema-constants.ts` | 案件狀態、議題類型、訂單來源等 **UI 標籤與色票**（副本） |
| `sample_*.masked.json` | 列表列、訊息、訂單摘要之 **示意結構** |

## 注意

示例 **不可** 當作真實 API 合約；實際欄位以執行時 API 為準。
